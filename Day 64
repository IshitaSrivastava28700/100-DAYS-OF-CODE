Q114: Write a program to take a string s as input. The task is to find the length of the longest substring without repeating characters. Print the length as output.

#include <stdio.h>
#include <string.h>

int longestUniqueSubstring(char s[]) {
    int n = strlen(s);
    int maxLen = 0;
    int start = 0;

    // Array to store last index of each character (-1 means not seen yet)
    int lastIndex[256];
    for (int i = 0; i < 256; i++) {
        lastIndex[i] = -1;
    }

    for (int end = 0; end < n; end++) {
        // If character already seen, move start to max of current start or last seen + 1
        if (lastIndex[(unsigned char)s[end]] >= start) {
            start = lastIndex[(unsigned char)s[end]] + 1;
        }

        // Update last index of current character
        lastIndex[(unsigned char)s[end]] = end;

        // Update max length
        int current


Input 1:
s = "abcabcbb"
Output 1:
3

Input 2:
s = "bbbbb"
Output 2:
1

Input 3:
s = "pwwkew"
Output 3:
3

