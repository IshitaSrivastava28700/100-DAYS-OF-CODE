Q95: Check if one string is a rotation of another.

#include <stdio.h>
#include <string.h>

int main() {
    char str1[100], str2[100], temp[200];

    printf("Enter first string: ");
    scanf("%s", str1);

    printf("Enter second string: ");
    scanf("%s", str2);

    // Check lengths first
    if (strlen(str1) != strlen(str2)) {
        printf("Strings are NOT rotations of each other.\n");
        return 0;
    }

    // Concatenate str1 with itself
    strcpy(temp, str1);
    strcat(temp, str1);

    // Check if str2 is a substring of temp
    if (strstr(temp, str2) != NULL) {
        printf("Strings ARE rotations of each other.\n");
    } else {
        printf("Strings are NOT rotations of each other.\n");
    }

    return 0;
}


Input 1:
abcde
deabc
Output 1:
Rotation

Input 2:
abc
acb
Output 2:
Not rotation



Q96: Reverse each word in a sentence without changing the word order.

#include <stdio.h>
#include <string.h>

void reverseWord(char *start, char *end) {
    while (start < end) {
        char temp = *start;
        *start = *end;
        *end = temp;
        start++;
        end--;
    }
}

int main() {
    char str[200];
    printf("Enter a sentence: ");
    scanf("%[^\n]", str);  // read sentence including spaces

    char *wordStart = NULL;
    char *ptr = str;

    while (*ptr) {
        if (wordStart == NULL && *ptr != ' ') {
            wordStart = ptr;  // mark start of word
        }
        if (wordStart && (*(ptr + 1) == ' ' || *(ptr + 1) == '\0')) {
            reverseWord(wordStart, ptr);  // reverse current word
            wordStart = NULL;
        }
        ptr++;
    }

    printf("Sentence with each word reversed:\n%s\n", str);

    return 0;
}


Input 1:
I love coding
Output 1:
I evol gnidoc

