Q111: Write a program to take an integer array arr and an integer k as inputs. The task is to find the first negative integer in each subarray of size k moving from left to right. If no negative exists in a window, print "0" for that window. Print the results separated by spaces as output.

#include <stdio.h>

void firstNegativeInSubarrays(int arr[], int n, int k) {
    for (int i = 0; i <= n - k; i++) {
        int firstNeg = 0;  // Default if no negative exists
        for (int j = i; j < i + k; j++) {
            if (arr[j] < 0) {
                firstNeg = arr[j];
                break;  // Found the first negative in this window
            }
        }
        printf("%d", firstNeg);
        if (i != n - k) {
            printf(" ");  // space separated output
        }
    }
    printf("\n");
}

int main() {
    int n, k;
    printf("Enter size of array: ");
    scanf("%d", &n);

    int arr[n];
    printf("Enter %d elements:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Enter subarray size k: ");
    scanf("%d", &k);

    if (k > n || k <= 0) {
        printf("Invalid input: k must be between 1 and n.\n");
    } else {
        printf("First negative integers in each subarray of size %d: ", k);
        firstNegativeInSubarrays(arr, n, k);
    }

    return 0;
}



Input 1:
arr[] = [-8, 2, 3, -6, 10], k = 2
Output 1:
-8 0 -6 -6

Input 2:
arr[] = [12, -1, -7, 8, -15, 30, 16, 28], k = 3
Output 2:
-1 -1 -7 -15 -15 0

Input 3:
arr[] = [12, 1, 3, 5], k = 3
Output 3:
0 0

