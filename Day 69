Q119: Write a program to take an integer array as input. Only one element will be repeated. Print the repeated element. Try to find the result in one single iteration.

#include <stdio.h>

int findRepeated(int arr[], int n) {
    int visited[n];  // auxiliary array to mark seen elements

    // Initialize visited array
    for (int i = 0; i < n; i++) {
        visited[i] = 0;
    }

    // Single iteration through arr
    for (int i = 0; i < n; i++) {
        if (visited[arr[i]] == 1) {
            return arr[i];  // Found repeated element
        }
        visited[arr[i]] = 1;
    }

    return -1;  // Should not happen if exactly one element is repeated
}

int main() {
    int n;
    printf("Enter size of array: ");
    scanf("%d", &n);

    int arr[n];
    printf("Enter %d elements:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int repeated = findRepeated(arr, n);
    printf("Repeated element: %d\n", repeated);

    return 0;
}



Input 1:
nums1 = [1,3,3,4]
Output 1:
3

Input 2:
nums1 = [1,2,2]
Output 2:
2

Input 3:
nums1 = [0,4,1,1,5]
Output 3:
1

